# ash
use `ash` to start
# installation
use this
```
mkdir -p $PREFIX/etc/apt/sources.list.d
echo "deb [trusted=yes] https://olan392.github.io/ash-repo stable main" > $PREFIX/etc/apt/sources.list.d/ash.list
pkg update
```
then use
```
pkg install ash
```
